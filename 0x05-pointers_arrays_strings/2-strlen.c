#include "main.h"
/**
 * _strlen - calculates string length
 * @s: a parameter to collect argument
 * Return: Alway 0.
*/
int _strlen(char *s)
{
int a, b = 0;
for (a = 0; a < *s; a++)
{
b++;
}
return (0);
}
