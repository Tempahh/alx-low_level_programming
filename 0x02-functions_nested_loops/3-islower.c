#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * islower - checks for lower case letters
 * Description- this program aims to find lower case haracters
*/
void islower(void)
{
char a;
if(a = 'a' && a <= 'z')
{
return (1);
}
return (0);
}