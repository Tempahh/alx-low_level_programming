#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * _isalpha - checks for alphabets
 *@c- collecting argument
 * Description- this program aims to find alphabets
 * Return: Always 0.
*/
int _isalpha(int c)
{
return ((c >= 65) && (c <= 122));
}
